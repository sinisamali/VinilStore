import { createMuiTheme } from '@material-ui/core/styles';



const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#000000', //black
    },
    secondary: {
      main: '#FFFFFF', //white
    },
  }
});
export default theme;